title: Accueil

pages: article
files: true

fields:
  title:
    label: Titre
    type:  text
  homeImage:
    label: Image pour la page d'accueil qui disparait au bout de quelques secondes
    type: selector
    mode: single
    required: true
  col1:
    label: Contenu de la colonne 1
    type: select
    options: query
    query: 
      page: /
      fetch: index
      value: '{{uid}}'
  col2:
    label: Contenu de la colonne 2
    type: select
    options: query
    query: 
      page: /
      fetch: index
      value: '{{uid}}'
  col3:
    label: Contenu de la colonne 3
    type: select
    options: query
    query: 
      page: /
      fetch: index
      value: '{{uid}}'